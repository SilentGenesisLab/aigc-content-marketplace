import { redirect } from "next/navigation";import { getSessionUser } from "@/lib/auth";import { AppShell } from "@/components/app-shell";import { OrderForm } from "@/components/order-form";
export default async function NewOrderPage(){const user=await getSessionUser();if(!user)redirect("/login");if(user.role!=="CLIENT")redirect("/dashboard");return <AppShell user={user}><div className="content"><div className="eyebrow">STANDARD BRIEF</div><h1>发布营销短视频订单</h1><p className="muted">锁定预算、交付标准和修改次数；中选并确认 Brief 后通过沙箱托管开工。</p><div className="card" style={{marginTop:22}}><OrderForm/></div></div></AppShell>}
