import { redirect } from "next/navigation";import { getSessionUser } from "@/lib/auth";import { AppShell } from "@/components/app-shell";import { OrderForm } from "@/components/order-form";
export default async function NewOrderPage(){const user=await getSessionUser();if(!user)redirect("/login");if(user.role==="CREATOR")redirect("/");return <AppShell user={user}><div className="content"><div className="eyebrow">STANDARD BRIEF</div><h1>发布营销短视频订单</h1><p className="muted">把交付标准一次说清。首周试运营不记录金额，也不经手支付。</p><div className="card" style={{marginTop:22}}><OrderForm/></div></div></AppShell>}
