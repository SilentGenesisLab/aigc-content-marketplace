import Link from "next/link";import { redirect } from "next/navigation";import { AppShell } from "@/components/app-shell";import { WorkflowCard } from "@/components/workflow-card";import { getSessionUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export default async function WorkPage(){const user=await getSessionUser();if(!user)redirect("/login");const where=user.role==="ADMIN"?{}:user.role==="CREATOR"?{creatorId:user.id}:{order:{clientId:user.id}};const rows=await prisma.subOrder.findMany({where,orderBy:{updatedAt:"desc"},include:{order:{select:{id:true,title:true,clientId:true,deadline:true}},creator:{select:{name:true}},briefSnapshot:true,deliveries:{orderBy:{version:"desc"},include:{compliance:true,revisions:true}},rightsReceipt:true,reviews:true}});const drafts=user.role!=="CREATOR"?await prisma.order.findMany({where:user.role==="ADMIN"?{status:"PENDING_REVIEW"}:{clientId:user.id,status:{in:["DRAFT","PENDING_REVIEW","OPEN"]}},orderBy:{updatedAt:"desc"}}):[];return <AppShell user={user}><div className="content"><div className="eyebrow">WORKSPACE</div><h1>{user.role==="CREATOR"?"我的制作":"我的订单"}</h1><p className="muted">每个状态都由真实操作推进，最终验收后才生成权利确认。</p>{drafts.length>0&&<section style={{margin:"24px 0"}}><h2 style={{marginBottom:12}}>发单进度</h2><div className="grid grid-3">{drafts.map(o=><Link className="card" href={`/orders/${o.id}`} key={o.id}><strong>{o.title}</strong><p className="muted">{o.code}</p><span className="chip blue">{o.status}</span></Link>)}</div></section>}<section className="grid" style={{marginTop:24}}>{rows.length?JSON.parse(JSON.stringify(rows)).map((item:never)=><WorkflowCard key={(item as {id:string}).id} item={item} user={user}/>):<div className="card empty">目前还没有履约子订单。</div>}</section></div></AppShell>}
