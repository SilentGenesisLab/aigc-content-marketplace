"use client";import {FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{api}from"@/lib/client";
export function ProfileForm({profile}:{profile:{bio:string;skills:string[];platforms:string[];styles:string[];available:boolean}|null}){const router=useRouter();const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/api/creator/profile",{method:"PATCH",body:JSON.stringify({bio:f.get("bio"),skills:String(f.get("skills")||"").split(/[,，]/),platforms:String(f.get("platforms")||"").split(/[,，]/),styles:String(f.get("styles")||"").split(/[,，]/),available:f.has("available")})});setMsg("档案已保存");router.refresh()}catch(e){setMsg(e instanceof Error?e.message:"保存失败")}}return <form className="form" onSubmit={submit}>{msg&&<div className="notice">{msg}</div>}<div className="field"><label>个人介绍</label><textarea className="input" name="bio" defaultValue={profile?.bio}/></div><div className="grid grid-3"><div className="field"><label>技能标签</label><input className="input" name="skills" defaultValue={profile?.skills.join("，")}/></div><div className="field"><label>擅长平台</label><input className="input" name="platforms" defaultValue={profile?.platforms.join("，")}/></div><div className="field"><label>内容风格</label><input className="input" name="styles" defaultValue={profile?.styles.join("，")}/></div></div><label><input type="checkbox" name="available" defaultChecked={profile?.available??true}/> 当前可接单</label><button className="button">保存档案</button></form>}
export function PortfolioForm(){const router=useRouter();const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/api/creator/profile",{method:"POST",body:JSON.stringify(Object.fromEntries(f.entries()))});(e.currentTarget).reset();setMsg("作品已添加");router.refresh()}catch(e){setMsg(e instanceof Error?e.message:"添加失败")}}return <form className="form" onSubmit={submit}>{msg&&<div className="notice">{msg}</div>}<div className="grid grid-2"><input className="input" name="title" required placeholder="作品名称"/><input className="input" name="fileUrl" type="url" required placeholder="作品地址"/></div><textarea className="input" name="description" placeholder="作品背景、你的职责和使用的工具"/><button className="button">添加真实作品</button></form>}
