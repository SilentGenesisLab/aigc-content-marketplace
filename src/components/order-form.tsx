"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "@/lib/client";
export function OrderForm(){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const p=Object.fromEntries(f.entries());try{const r=await api<{order:{id:string}}>("/api/orders",{method:"POST",body:JSON.stringify({...p,quantity:Number(p.quantity),slots:Number(p.slots),sampleRequired:f.has("sampleRequired"),referenceUrls:String(p.referenceUrls||"").split("\n").filter(Boolean)})});router.push(`/orders/${r.order.id}`);router.refresh()}catch(e){setError(e instanceof Error?e.message:"创建失败")}finally{setLoading(false)}}return <form className="form" onSubmit={submit}>{error&&<div className="notice error">{error}</div>}<div className="grid grid-2"><div className="field"><label>订单标题</label><input className="input" name="title" required placeholder="例：秋季新品种草短视频"/></div><div className="field"><label>内容主题</label><input className="input" name="topic" required placeholder="产品和核心主题"/></div></div><div className="field"><label>营销目标</label><textarea className="input" name="objective" required placeholder="希望解决什么问题，期望用户采取什么行动"/></div><div className="grid grid-4"><div className="field"><label>成片数量</label><input className="input" name="quantity" type="number" min="1" max="100" defaultValue="1" required/></div><div className="field"><label>单条时长</label><input className="input" name="duration" defaultValue="30–60 秒" required/></div><div className="field"><label>发布平台</label><select className="input" name="platform"><option>抖音</option><option>小红书</option><option>视频号</option><option>B站</option></select></div><div className="field"><label>画幅</label><select className="input" name="aspectRatio"><option>9:16 竖屏</option><option>16:9 横屏</option><option>1:1 方形</option></select></div></div><div className="grid grid-2"><div className="field"><label>目标受众</label><input className="input" name="audience" required placeholder="年龄、兴趣或消费场景"/></div><div className="field"><label>内容风格</label><input className="input" name="style" required placeholder="例：真实测评、轻快口播"/></div></div><div className="grid grid-2"><div className="field"><label>订单模式</label><select className="input" name="mode"><option value="MULTI_DELIVERY">多人独立交付</option><option value="CONTEST">竞稿择优</option></select></div><div className="field"><label>中选名额</label><input className="input" name="slots" type="number" min="1" max="50" defaultValue="1" required/></div></div><div className="field"><label>参考文件地址（每行一个）</label><textarea className="input" name="referenceUrls" placeholder="可填写已上传到企业存储的文件地址"/></div><div className="field"><label>禁用元素</label><textarea className="input" name="forbiddenElements" placeholder="不得出现的品牌、人物、表达或视觉元素"/></div><div className="field"><label>验收标准</label><textarea className="input" name="acceptanceCriteria" required placeholder="逐条说明分辨率、时长、内容和版权要求"/></div><div className="grid grid-2"><div className="field"><label>交付截止时间</label><input className="input" name="deadline" type="datetime-local" required/></div><label className="row" style={{alignSelf:"end",padding:"11px 0"}}><input name="sampleRequired" type="checkbox"/> 报名时需要样稿</label></div><div className="row"><button className="button" disabled={loading}>{loading?"创建中…":"保存为草稿"}</button><span className="muted" style={{fontSize:13}}>创建后需提交平台审核，审核通过才会公开。</span></div></form>}
